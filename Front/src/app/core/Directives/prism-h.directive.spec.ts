import { PrismHDirective } from './prism-h.directive';

describe('PrismHDirective', () => {
  it('should create an instance', () => {
    const directive = new PrismHDirective();
    expect(directive).toBeTruthy();
  });
});
