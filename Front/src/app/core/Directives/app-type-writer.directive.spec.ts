import { AppTypeWriterDirective } from './app-type-writer.directive';

describe('AppTypeWriterDirective', () => {
  it('should create an instance', () => {
    const directive = new AppTypeWriterDirective();
    expect(directive).toBeTruthy();
  });
});
